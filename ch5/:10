#include <stdio.h>
#include <string.h>
#define MAXLINE 1000

int main(int argc, char *argv[])
{
	int found = 0;
	char* target = "monkey apple banana jenny";

	 if (strstr(target, argv[1])!= NULL && strstr(target, argv[2]))
	 {
		printf("%s\n", target);
 	   	found++;
	 }
     return found;
}
